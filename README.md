This is project intends to be able to display png images as ascii art in a terminal using ncurses.
Later iterations of this project can attempt to display videos by displaying a sequence of individual frames and allow browsing of a directory.

Current usage:
Compilation with: g++ tiv.cpp -o tiv -lncurses
